{% extends 'base.html.twig' %}

{% block title %}FieldOfStudy{% endblock %}

{% block body %}
    <h1>FieldOfStudy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field_of_study.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ field_of_study.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_field_of_study_index') }}">back to list</a>

    <a href="{{ path('app_field_of_study_edit', {'id': field_of_study.id}) }}">edit</a>

    {{ include('field_of_study/_delete_form.html.twig') }}
{% endblock %}
