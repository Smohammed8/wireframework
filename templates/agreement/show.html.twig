{% extends 'base.html.twig' %}

{% block title %}Agreement{% endblock %}

{% block body %}
    <h1>Agreement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agreement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ agreement.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agreement_index') }}">back to list</a>

    <a href="{{ path('app_agreement_edit', {'id': agreement.id}) }}">edit</a>

    {{ include('agreement/_delete_form.html.twig') }}
{% endblock %}
