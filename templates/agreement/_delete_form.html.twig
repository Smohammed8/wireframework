<form method="post" action="{{ path('app_agreement_delete', {'id': agreement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agreement.id) }}">
    <button class="btn">Delete</button>
</form>
