{% extends 'base.html.twig' %}

{% block title %}Woreda index{% endblock %}

{% block body %}
    <h1>Woreda index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for woreda in woredas %}
            <tr>
                <td>{{ woreda.id }}</td>
                <td>{{ woreda.name }}</td>
                <td>
                    <a href="{{ path('app_woreda_show', {'id': woreda.id}) }}">show</a>
                    <a href="{{ path('app_woreda_edit', {'id': woreda.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_woreda_new') }}">Create new</a>
{% endblock %}
