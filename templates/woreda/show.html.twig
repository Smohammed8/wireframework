{% extends 'base.html.twig' %}

{% block title %}Woreda{% endblock %}

{% block body %}
    <h1>Woreda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ woreda.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ woreda.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_woreda_index') }}">back to list</a>

    <a href="{{ path('app_woreda_edit', {'id': woreda.id}) }}">edit</a>

    {{ include('woreda/_delete_form.html.twig') }}
{% endblock %}
