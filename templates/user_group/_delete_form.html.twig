<form method="post" action="{{ path('app_user_group_delete', {'id': user_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_group.id) }}">

    <button class="btn btn-outline-secondary btn-sm" >  <i class=" fa fa-trash"> </i> Delete</button>
</form>
