{% extends 'base.html.twig' %}

{% block title %}StudentPayment{% endblock %}

{% block body %}
    <h1>StudentPayment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_payment.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ student_payment.year }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ student_payment.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_student_payment_index') }}">back to list</a>

    <a href="{{ path('app_student_payment_edit', {'id': student_payment.id}) }}">edit</a>

    {{ include('student_payment/_delete_form.html.twig') }}
{% endblock %}
