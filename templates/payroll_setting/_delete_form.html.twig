<form method="post" action="{{ path('app_payroll_setting_delete', {'id': payroll_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payroll_setting.id) }}">
    <button class="btn btn-outline-secondary btn-sm"> <i class="fa fa-trash"> </i> Delete</button>
</form>
