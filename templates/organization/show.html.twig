{% extends 'base.html.twig' %}

{% block title %}Organization{% endblock %}

{% block body %}
    <h1>Organization</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organization.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organization.name }}</td>
            </tr>
            <tr>
                <th>Moto</th>
                <td>{{ organization.moto }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ organization.logo }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ organization.tel }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ organization.fax }}</td>
            </tr>
            <tr>
                <th>Pobox</th>
                <td>{{ organization.pobox }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organization_index') }}">back to list</a>

    <a href="{{ path('app_organization_edit', {'id': organization.id}) }}">edit</a>

    {{ include('organization/_delete_form.html.twig') }}
{% endblock %}
