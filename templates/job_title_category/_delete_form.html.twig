<form method="post" action="{{ path('app_job_title_category_delete', {'id': job_title_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_title_category.id) }}">
    <button  class="btn btn-outline-secondary btn-sm mr-1" > <i class="fa fa-trash"></i> Delete</button>
</form>
