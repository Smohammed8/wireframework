{% extends 'base.html.twig' %}

{% block title %}JobTitleCategory{% endblock %}

{% block body %}
    <h1>JobTitleCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_title_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_title_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_title_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_title_category_index') }}">back to list</a>

    <a href="{{ path('app_job_title_category_edit', {'id': job_title_category.id}) }}">edit</a>

    {{ include('job_title_category/_delete_form.html.twig') }}
{% endblock %}
