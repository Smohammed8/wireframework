{% extends 'base.html.twig' %}

{% block title %}PaymentSession{% endblock %}

{% block body %}
    <h1>PaymentSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_session.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ payment_session.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_session_index') }}">back to list</a>

    <a href="{{ path('app_payment_session_edit', {'id': payment_session.id}) }}">edit</a>

    {{ include('payment_session/_delete_form.html.twig') }}
{% endblock %}
