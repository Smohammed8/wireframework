<form method="post" action="{{ path('app_employee_education_delete', {'id': employee_education.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_education.id) }}">
    <button class="btn  btn-sm btn-outline-secondary  mr-1"> <i class="far fa-trash-alt"></i>  Delete</button>
</form>


