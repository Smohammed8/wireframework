{% extends 'base.html.twig' %}

{% block title %}PositionCode{% endblock %}

{% block body %}
    <h1>PositionCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ position_code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ position_code.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_position_code_index') }}">back to list</a>

    <a href="{{ path('app_position_code_edit', {'id': position_code.id}) }}">edit</a>

    {{ include('position_code/_delete_form.html.twig') }}
{% endblock %}
