<form method="post" action="{{ path('app_position_code_delete', {'id': position_code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ position_code.id) }}">
    <button class="btn">Delete</button>
</form>
