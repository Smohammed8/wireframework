{% extends 'base.html.twig' %}

{% block title %}TeacherEducation{% endblock %}

{% block body %}
    <h1>TeacherEducation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher_education.id }}</td>
            </tr>
            <tr>
                <th>Major</th>
                <td>{{ teacher_education.major }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_education_index') }}">back to list</a>

    <a href="{{ path('app_teacher_education_edit', {'id': teacher_education.id}) }}">edit</a>

    {{ include('teacher_education/_delete_form.html.twig') }}
{% endblock %}
