<form method="post" action="{{ path('app_student_parent_delete', {'id': student_parent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_parent.id) }}">
    <button class="btn btn-outline-primary btn-sm mr-1"> <i class="fa fa-trash-alt"> </i>Delete</button>
</form>
