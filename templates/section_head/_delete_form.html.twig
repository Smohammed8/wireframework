<form method="post" action="{{ path('app_section_head_delete', {'id': section_head.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section_head.id) }}">
    <button class="btn btn-outline-secondary btn-sm mr-1"> <i class="fa fa-trash-alt"> </i>Delete</button>
</form>
