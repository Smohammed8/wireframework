{% extends 'base.html.twig' %}

{% block title %}SubjectAssignment{% endblock %}

{% block body %}
    <h1>SubjectAssignment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject_assignment.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ subject_assignment.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subject_assignment_index') }}">back to list</a>

    <a href="{{ path('app_subject_assignment_edit', {'id': subject_assignment.id}) }}">edit</a>

    {{ include('subject_assignment/_delete_form.html.twig') }}
{% endblock %}
